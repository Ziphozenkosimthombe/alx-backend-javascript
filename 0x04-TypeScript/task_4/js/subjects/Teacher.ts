/**
 * Create a file Teacher.ts and write a Teacher interface in a namespace named Subjects.
 * The interface should have two properties: firstName and lastName, both of type string.
 * */ 

namespace Subjects{
    export interface Teacher{
        firstName: string;
        lastName: string;
    }
}